modules:
  - type: default-flatpaks
    notify: false
    system:
      install:
        - com.mattjakeman.ExtensionManager

  - type: gnome-extensions
    install:
      - 7048

  - type: gschema-overrides
    include:
      - zz1-00-cyanite.gschema.override

  - type: bling
    install:
      - dconf-update-service